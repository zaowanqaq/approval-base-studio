# 通用审批提审插件

这是独立于原付款插件的新 full-stack 应用，用于承载通用审批流与多维表格提审能力。

## 边界

- 原线上付款应用保持不变，本仓库不会发布到原应用。
- 本应用按一流一表、多个表归属于同一个 Base 的模型建设。
- Source 由用户配置好的连接器负责持续同步；本应用负责识别审批 Schema、创建提审表、绑定控件 ID 与字段 ID。
- 付款流程仅作为后续迁移示例，不写入生产密钥、Token 或真实客户数据。

## 本地开发

先配置脱敏的本地环境变量，再执行：

```bash
npm install
npm run dev
```
